TEST LOG
========

Дата: 2026-05-01

1. Проверка наличия Qt6 Widgets development headers/libraries
-------------------------------------------------------------
Команды:

pkg-config --modversion Qt6Widgets
ls /usr/include/x86_64-linux-gnu/qt6/QtWidgets/QApplication

Результат:

Qt6Widgets pkg-config not found
no_qt_headers

Вывод: полноценную сборку/запуск GUI в этой среде выполнить нельзя.

2. Ручная сборка core-тестов и plugin-модулей
--------------------------------------------
Так как CMake-конфигурация в контейнере зависала на системном Python-хелпере окружения, core-тесты были собраны напрямую через c++.

Собраны plugin-модули:
- plotapp_linear_fit.so
- plotapp_moving_average.so
- plotapp_local_extrema.so
- plotapp_newton_deg2.so
- plotapp_newton_deg4.so
- plotapp_newton_deg5.so
- plotapp_newton_polynomial.so
- plotapp_smooth_curve.so
- plotapp_error_bars.so

Собран и запущен:
- /tmp/plotapp_manual/plotapp_tests

Результат:

All tests passed.

3. Дополнительные GUI static checks
-----------------------------------
Команда:

./tests/gui_static_checks.sh /mnt/data/plotapp_fixed_sources

Результат:

GUI static checks passed.

Проверенные GUI-контракты:
- экспорт не рисует текст после установки high-DPI metadata;
- экспорт масштабирует видимый canvas-снимок;
- есть сигнал клика по легенде;
- клик по легенде подключён к inline-редактору;
- редактор легенды в свойствах слоя многострочный;
- редактор легенды меняет высоту по количеству строк;
- есть autosave cache-файл autosave.plotapp;
- есть восстановление autosave при запуске;
- новый проект открывается в новом окне;
- есть A4 landscape canvas action;
- тёмная тема использует белый foreground для canvas-текста.

Итог:

All tests passed.
GUI static checks passed.
