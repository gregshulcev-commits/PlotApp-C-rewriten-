Что не удалось проверить полностью
===============================

1. Не удалось выполнить полноценную сборку и запуск Qt GUI-приложения в этой среде.

Причина:
- отсутствуют Qt6 Widgets development headers/libraries;
- проверка среды показала:

  Qt6Widgets pkg-config not found
  no_qt_headers

Из-за этого не удалось открыть настоящее окно PlotApp и вручную проверить:
- визуальный экспорт PNG/SVG из GUI при 640 dpi;
- кнопку A4 screenshot в живом окне;
- редактирование легенды кликом мыши в живом окне;
- восстановление autosave после аварийного закрытия GUI.

2. CMake-конфигурация в контейнере была нестабильна из-за зависания системного Python-хелпера окружения при проверке временных файлов CMake. Чтобы не блокироваться, проверка core-тестов выполнена ручной сборкой через c++ без CMake.

Что всё равно проверено
=======================

1. Ручной сборкой собраны:
- core-тестовый бинарник plotapp_tests;
- plugin .so модули, необходимые тестам.

2. Запущен существующий набор тестов:

  All tests passed.

3. Запущен дополнительный статический GUI-регрессионный тест:

  GUI static checks passed.

Исходники приложены полностью, включая изменения GUI и этот отчёт.
